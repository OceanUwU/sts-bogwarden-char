{
  "modid": "${ModID}",
  "name": "${project.name}",
  "author_list": [
    "occultpyromancer",
    "ocean"
  ],
  "credits": "Full credits for SFX can be found on the Steam workshop page.",
  "description": "${project.description}",
  "version": "${project.version}",
  "sts_version": "${SlayTheSpire.version}",
  "mts_version": "${ModTheSpire.version}",
  "dependencies": [
    "basemod",
    "stslib"
  ],
  "optional_dependencies": ["aspiration"]
}

